# Copy to .env (or export directly) to point app/db/* scripts at your Postgres.
# The default below matches docker-compose.yml, so `docker compose up -d` with
# no .env at all already works.
DATABASE_URL=postgresql+psycopg2://kseb:kseb@localhost:5433/kseb
